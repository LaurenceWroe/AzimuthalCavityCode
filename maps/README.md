# Local external maps

Dense maps are intentionally absent and ignored by Git. Set `AZIMUTHAL_MAP_DIR` or `cfg.map_root` to their location. See [map schema, filenames and recovery procedure](../docs/MAPS.md). A small explicitly synthetic fixture is tracked under `examples/data/`.
